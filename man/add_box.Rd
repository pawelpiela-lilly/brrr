% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_box.R
\name{add_box}
\alias{add_box}
\title{Add a box to a plot}
\usage{
add_box(
  obj,
  spacing,
  n_categories,
  single_category_height,
  neutral_pos,
  n_ticks,
  from,
  to,
  label = NULL,
  logscale = FALSE,
  b = 10,
  show_axis = TRUE,
  arrow_labels = NULL,
  direction = "up",
  userect = FALSE,
  vline = NULL,
  colbreaks = NULL
)
}
\arguments{
\item{obj}{The object to which the box will be added (header or box object).}

\item{spacing}{Numeric. The spacing between the box and the object above it.}

\item{n_categories}{Integer. The number of categories in the box.}

\item{single_category_height}{Numeric or unit. The height of each category.}

\item{neutral_pos}{Integer. The position of the neutral point.}

\item{n_ticks}{Integer. The number of ticks on the axis.}

\item{from}{Numeric. The starting value of the axis range.}

\item{to}{Numeric. The ending value of the axis range.}

\item{label}{Character or expression. The label for the axis. Default is NULL.}

\item{logscale}{Logical. Whether to use logarithmic scale. Default is FALSE.}

\item{b}{Numeric. The base for logarithmic scale. Default is 10.}

\item{show_axis}{Logical. Whether to show the axis. Default is TRUE.}

\item{arrow_labels}{Character vector of length 2. Labels for benefit arrows.
Default is NULL (no arrows). Set to add directional arrows.}

\item{direction}{Character. Direction of benefit arrows ('up' or 'down'). Default is 'up'.}

\item{userect}{Logical. Whether to use rectangles instead of lines. Default is FALSE.}

\item{vline}{Numeric. Position of an optional vertical reference line. Default is NULL.}

\item{colbreaks}{Numeric vector. Column break positions for separation lines. Default is NULL.}
}
\value{
A list containing:
\describe{
\item{label_fun}{Function to add labels to the box}
\item{box}{The box object}
\item{name}{Character string "box" identifying the object type}
\item{userect}{Logical indicating rectangle usage}
\item{header}{Reference to the header object}
\item{y_pos}{The y-position of the bottom of the box}
}
}
\description{
This function adds a box to a plot. The box can be connected to either a header
or another box, creating stacked visualizations.
}
\examples{
\dontrun{
grid.newpage()

# Create header
my_header <- create_header(c(0.2, 0.3, 0.5), c("Endpoint", "Values", "Comparison"))

# Add a box
box1 <- add_box(my_header, 0, 3, 0.05, 3, 5, 0, 1, "Effect Size",
                arrow_labels = c("Favors Treatment", "Favors Control"))

# Add labels
box1$label_fun("Endpoint 1", 1, 1)
box1$label_fun("Value 1", 2, 1)
}

}
