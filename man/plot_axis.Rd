% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis.R
\name{plot_axis}
\alias{plot_axis}
\title{Plot Axis}
\usage{
plot_axis(
  xlength,
  xpos,
  ypos,
  from,
  to,
  n_ticks,
  neutral_pos,
  label = NULL,
  logscale = FALSE,
  b = 10,
  show_axis = TRUE,
  secondary_ticks = TRUE,
  options = page_options$new()
)
}
\arguments{
\item{xlength}{Numeric. The length of the axis line (0-1 scale).}

\item{xpos}{Numeric. The x-coordinate of the starting position of the axis line (0-1).}

\item{ypos}{Numeric. The y-coordinate of the starting position of the axis line (0-1).}

\item{from}{Numeric. The starting value of the axis range.}

\item{to}{Numeric. The ending value of the axis range.}

\item{n_ticks}{Integer. The number of ticks on the axis (must be >= 2).}

\item{neutral_pos}{Integer. The position of the neutral tick (0 on linear scale, 1 on log scale).}

\item{label}{Character or expression. The label for the axis. Default is NULL.}

\item{logscale}{Logical. Whether to use logarithmic scale. Default is FALSE.}

\item{b}{Numeric. The base for logarithmic scale. Default is 10.}

\item{show_axis}{Logical. Whether to show the axis. Default is TRUE.}

\item{secondary_ticks}{Logical. Whether to show secondary ticks. Default is TRUE.}

\item{options}{A page_options object. Default creates new page_options.}
}
\value{
A list containing:
\describe{
\item{axis_function}{A function that scales a value to the position on the axis}
\item{from}{The transformed starting value of the axis range}
\item{to}{The transformed ending value of the axis range}
\item{length}{The length of the axis line}
}
}
\description{
This function plots an axis with ticks and labels on a given plot.
}
\examples{
\dontrun{
grid.newpage()

# Linear axis
pp_lin_axis <- plot_axis(xlength = 0.8, xpos = 0.5, ypos = 0.5, 
         from = 0, to = 10, n_ticks = 6, neutral_pos = 1)

# Reversed axis
pp_rev_axis <- plot_axis(xlength = 0.8, xpos = 0.5, ypos = 0.4, 
         from = 10, to = 0, n_ticks = 6, neutral_pos = 1)

# Logarithmic axis
pp_log_axis <- plot_axis(xlength = 0.8, xpos = 0.5, ypos = 0.3, 
         from = 0.1, to = 1000, b = 10, n_ticks = 6, neutral_pos = 1, 
         logscale = TRUE, label = "Log Scale (0.1 to 1000)")
}

}
